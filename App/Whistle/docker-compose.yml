version: '3.7'
services:
  whistle:
    restart: always
    container_name: Whistle
    image:
      whistle:latest
    build:
      ./
    volumes:
      - /opt/DockerStore/Volumes/Whistle:/volume
    ports:
      - "9103:9103"