version: '3.7'
services:
  redis:
    restart: always
    container_name: Redis
    image:
      redis:latest
    build:
      ./
    volumes:
      - /opt/DockerStore/Volumes/Redis/db:/volume/db
      - /opt/DockerStore/Volumes/Redis/log:/volume/log
    ports:
      - "6379:6379"