version: '3.7'
services:
  phpmyadmin:
    restart: always
    container_name: phpMyAdmin
    image:
      phpmyadmin:latest
    build:
      ./
    ports:
      - "9002:9002"
    environment:
      PMA_ARBITRARY: 1