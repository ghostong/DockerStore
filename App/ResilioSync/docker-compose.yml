version: '3.7'
services:
  resiliosync:
    restart: always
    container_name: ResilioSync
    image:
      resiliosync:latest
    build:
      ./
    volumes:
      - ~/DockerStore/Volumes/ResilioSync:/volume
    ports:
      - "9101:9101"
      - "55555:55555"